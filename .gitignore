# depot is vendored in as its own git checkout for map generation
depot/

# Map build outputs and inputs -- these ship as GitHub release assets,
# not as repo contents.
build/.venv/
build/data/
build/dist/
build/CHS/
build/CHS_demand/
build/source/
build/*.osm.pbf

# US Demand Generator is cloned in as its own checkout
us-demand/

# old-bbox build outputs kept around while iterating
build/CHS_oldbbox_v1.1.0/

__pycache__/
*.pyc

# Prior-extent build outputs kept for reference, not shipped
build/CHS_tricounty_v1.4.1/
